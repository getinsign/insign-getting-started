{
  "name": "sig-funnel",
  "version": "1.0.0",
  "description": "Broker Mandate Signature Funnel with inSign Integration",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "npx playwright test --config=tests/playwright.config.js --reporter=list",
    "test:headed": "npx playwright test --config=tests/playwright.config.js --headed --reporter=list",
    "test:debug": "npx playwright test --config=tests/playwright.config.js --debug"
  },
  "dependencies": {
    "axios": "^1.18.1",
    "express": "^5.2.1",
    "form-data": "^4.0.6",
    "pdfkit": "^0.19.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "opentype.js": "^2.0.0"
  }
}
